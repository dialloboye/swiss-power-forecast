[project]
name = "elecprice"
version = "0.1.0"
description = "Probabilistic day-ahead electricity price forecasting (CH)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
